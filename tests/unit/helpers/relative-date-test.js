import { relativeDate } from '../../../helpers/relative-date';
import { module, test } from 'qunit';

module('Unit | Helper | relative date');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = relativeDate(42);
  assert.ok(result);
});
