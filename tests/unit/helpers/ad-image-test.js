import { adImage } from '../../../helpers/ad-image';
import { module, test } from 'qunit';

module('Unit | Helper | ad image');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = adImage(42);
  assert.ok(result);
});
