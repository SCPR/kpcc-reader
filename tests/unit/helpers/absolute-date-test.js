import { absoluteDate } from '../../../helpers/absolute-date';
import { module, test } from 'qunit';

module('Unit | Helper | absolute date');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = absoluteDate(42);
  assert.ok(result);
});
