import { assetUrl } from '../../../helpers/asset-url';
import { module, test } from 'qunit';

module('Unit | Helper | asset url');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = assetUrl(42);
  assert.ok(result);
});
